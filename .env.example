# Backend port (used by backend/main.py when running `python backend/main.py`)
PORT=8000

# Backend origin exposed to the frontend
VITE_API_URL=http://localhost:8000

# Frontend dev-server port (used by `npm run dev` / `npm run start`)
FRONTEND_PORT=3000
